# Copy to .env and adjust if you're running the backend outside Docker
# (e.g. `uvicorn app.main:app --reload` during development).
# Inside Docker this is set directly in docker-compose.yml.
DATABASE_URL=sqlite:///./local.db
